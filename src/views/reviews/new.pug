extends /partials/layout

block content
  div.new-review
    h1 New review for #{album.title}
    form.flex-full(action=`/reviews/`, method="post")
      div
        textarea.new-review-textarea(type="text", name="new-review")
      div 
        input.new-review-submit-btn(type="submit", placeholder="Submit")
    
